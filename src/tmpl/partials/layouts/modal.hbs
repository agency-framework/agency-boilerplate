---
class: #string
deep: "modal"
ajax: #"ajax/"
---
<aside class="partial controller{{#if class}} {{class}}{{/if}}" data-partial="layouts/modal" data-controller="./partials/layouts/Modal" data-deep="{{deep}}"{{#if ajax}} data-ajax="{{ajax}}"{{/if}}>
  {{#block "spinner"}}{{/block}}

  {{{mixin "agency-pkg-element-base/link/deep" class="close" deep=deep url="#" title="&times;"}}}
  <div class="content">
    <div class="document">
      {{#block "content"}}

      {{/block}}
    </div>
  </div>
</aside>
